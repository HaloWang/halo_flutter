name: halo
description: Personal flutter all-in-one package.
version: 0.7.2
homepage: https://github.com/HaloWang/halo_flutter

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

  collection: # https://pub.dev/packages/collection/
  stack_trace: # https://pub.dev/packages/stack_trace/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
